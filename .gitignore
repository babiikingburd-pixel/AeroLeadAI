# Env / secrets — NEVER commit these (see 04-SECURITY-NOTES.md)
.env
.env.local
.env*.local

# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Replit internals — never belongs in the repo or shared zips
.local/
.config/
.cache/
.upm/

# Standalone validator — local secrets, local SQLite state, local venv
tools/standalone-validator/config.env
tools/standalone-validator/*.db
tools/standalone-validator/venv/

# Misc
.DS_Store
*.log
zipFile.zip
__pycache__/
*.pyc
.pytest_cache/
