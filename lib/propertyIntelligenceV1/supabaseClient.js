// Owner-only clean database: property intelligence persists through server
// APIs; this legacy browser adapter deliberately falls back to localStorage.
const supabase = null;

export default supabase;
